# SuperSecretSettings: Pre 1.9 InGame Shaders (They never removed them lmao)

## *Based around the Lambda Plugin SDK*
### *To build the plugin, simply run* `./gradlew jar` *or the* `jar` *task in your IDE.*